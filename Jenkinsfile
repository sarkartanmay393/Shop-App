pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Building stage is running."
            }
        }
        stage('test') {
            steps {
                echo "test stage is running."
            }
        }
        stage('deploy') {
            steps {
                echo "Building stage is running."
            }
        }
    }
}
