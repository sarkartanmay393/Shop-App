import 'package:flutter/material.dart';

class Offers extends StatelessWidget {
  static const routeName = "/offers";


  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Offers page under construction."),
    );
  }
}