import 'package:flutter/material.dart';

class Categories extends StatelessWidget {
  static const routeName = "/categories";


  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Dining page under construction."),
    );
  }
}
