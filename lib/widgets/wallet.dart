import 'package:flutter/material.dart';

class Wallet extends StatelessWidget {
  static const routeName = "/wallet";


  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Wallet page under construction."),
    );
  }
}